@model ICollection<BnbGo.Models.RentType>
@Html.AntiForgeryToken()
@if (Model != null && Model.Count() > 0)
{
    <table class="col col-xl-12 col-la-12 col-md-12 col-sm-12 col-xs-12">
        <thead>
          <tr>
            <th>Element</th>
            <th>Value</th>
          </tr>
        </thead>
        <tbody>
            @foreach (var renttype in Model)
            {
                <tr>
                    <td>Id</td>
                    <td>@renttype.Id</td>
                </tr>
                <tr>
                    <td>Name</td>
                    <td>@renttype.Name</td>
                </tr>
                <tr>
                    <td>Description</td>
                    <td>@renttype.Description</td>
                </tr>
                <tr>
                    <td>Create date</td>
                    <td>@renttype.CreatedAt</td>
                </tr>
                <tr>
                    <td>Update date</td>
                    @if (@renttype.UpdatedAt == null) {
                        <td>Not Updated</td>
                    } else {
                        <td>@renttype.UpdatedAt</td>
                    }
                </tr>
                <tr>
                    <td>Delete date</td>
                    @if (@renttype.DeletedAt == null) {
                        <td>Not Deleted</td>
                    } else {
                        <td>@renttype.DeletedAt</td>
                    }
                </tr>
            } 
        </tbody>
      </table>
}
else
{
    <p>No details available!</p>
}
</div>