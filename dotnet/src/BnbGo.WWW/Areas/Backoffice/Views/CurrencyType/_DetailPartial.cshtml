@model ICollection<BnbGo.Models.CurrencyType>
@Html.AntiForgeryToken()
@if (Model != null && Model.Count() > 0)
{
    <table class="col col-xl-12 col-la-12 col-md-12 col-sm-12 col-xs-12">
        <thead>
          <tr>
            <th>Element</th>
            <th>Value</th>
          </tr>
        </thead>
        <tbody>
            @foreach (var currencytype in Model)
            {
                <tr>
                    <td>Id</td>
                    <td>@currencytype.Id</td>
                </tr>
                <tr>
                    <td>Name</td>
                    <td>@currencytype.Name</td>
                </tr>
                <tr>
                    <td>Create date</td>
                    <td>@currencytype.CreatedAt</td>
                </tr>
                <tr>
                    <td>Update date</td>
                    @if (@currencytype.UpdatedAt == null) {
                        <td>Not Updated</td>
                    } else {
                        <td>@currencytype.UpdatedAt</td>
                    }
                </tr>
                <tr>
                    <td>Delete date</td>
                    @if (@currencytype.DeletedAt == null) {
                        <td>Not Deleted</td>
                    } else {
                        <td>@currencytype.DeletedAt</td>
                    }
                </tr>
            } 
        </tbody>
      </table>
}
else
{
    <p>No details available!</p>
}
</div>