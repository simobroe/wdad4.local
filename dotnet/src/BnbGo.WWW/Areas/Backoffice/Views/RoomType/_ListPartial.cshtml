@model ICollection<BnbGo.Models.RoomType>
@Html.AntiForgeryToken()
@if (Model != null && Model.Count() > 0)
{
      <table class="col col-xl-12 col-la-12 col-md-12 col-sm-12 col-xs-12">
        <thead>
          <tr>
            <th>RoomType</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          @foreach (var roomtype in Model)
          {
            <tr>
              <td>@roomtype.Name</td>
              <td>
                <a asp-controller="roomtype" asp-action="Show" asp-route-id="@roomtype.Id">
                  <i class="fa fa-eye" title="View"></i>
                </a>
                <a asp-controller="roomtype" asp-action="Edit" asp-route-id="@roomtype.Id">
                  <i class="fa fa-pencil" title="Edit"></i>
                </a>
                @if (@roomtype.DeletedAt != null)
                {
                    <a asp-controller="roomtype" asp-action="Delete" asp-route-id="@roomtype.Id" asp-route-actionType="5" data-is-softdelete="true">
                      <i class="fa fa-trash-o" title="revert delete"></i>
                    </a>
                }
                else
                {
                    <a asp-controller="roomtype" asp-action="Delete" asp-route-id="@roomtype.Id" asp-route-actionType="4">
                      <i class="fa fa-trash" title="soft delete"></i>
                    </a>
                }
                <a asp-controller="roomtype" asp-action="Delete" asp-route-id="@roomtype.Id" asp-route-actionType="3">
                  <i class="fa fa-ban" title="delete forever"></i>
                </a>
              </td>
            </tr>
          }
        </tbody>
      </table>
}
else
{
    <p>No RoomTypees in the database!</p>
}
</div>